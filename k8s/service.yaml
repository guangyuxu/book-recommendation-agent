apiVersion: v1
kind: Service
metadata:
  name: book-recommendation-agent
  namespace: book-agent
  labels:
    app: book-recommendation-agent
spec:
  type: ClusterIP
  selector:
    app: book-recommendation-agent
  ports:
    - name: http
      port: 8000
      targetPort: 8000
