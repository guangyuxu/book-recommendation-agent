"""Placeholder for integration tests.

Empty on purpose: end-to-end tests (real DB + Anthropic) go here when needed. Gate each behind
`RUN_INTEGRATION=1` so a normal `make test` never hits the network, and run them explicitly with
`RUN_INTEGRATION=1 python -m pytest tests/integration_tests`.
"""
